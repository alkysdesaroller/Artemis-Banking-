@model decimal

<div class="card">
    <div class="card-header">
        <div class="card-title text-muted h4">Deuda promedio del sistema:</div>
    </div>
    <div class="card-body">
        <div class="card-text h4">@(Model.ToString("F2"))</div>
    </div>
</div>

